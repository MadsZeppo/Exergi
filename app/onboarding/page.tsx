import { MerchantPage, Status } from "../merchant-shell";
export default function Page() { return <MerchantPage eyebrow="Connect data" title="Build a trustworthy customer record" description="Connections are not marked ready until reconciliation and temporal checks pass."><div className="card-grid">{[["Shopify","Contract ready","partial"],["Klaviyo","Contract ready","partial"],["Web events","Endpoint ready","good"],["Cost data","CSV template ready","good"]].map(([name,state,tone])=><article className="product-card" key={name}><Status label={state} tone={tone}/><h2>{name}</h2><p>Configure a secret reference or import validated source data.</p><button>Configure</button></article>)}</div></MerchantPage> }
