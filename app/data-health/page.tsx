import { MerchantPage, Status } from "../merchant-shell";
export default function Page() { const rows=[["Orders","VERIFIED","good"],["Refunds","VERIFIED","good"],["Customer identity","94% resolved","partial"],["Behavioral history","PARTIAL","partial"],["Randomized assignments","AVAILABLE","good"],["COGS","100% demo coverage","good"]]; return <MerchantPage eyebrow="Data trust" title="Capability-specific readiness" description="There is no composite trust score. Each product capability has its own evidence boundary."><section className="product-panel">{rows.map(([a,b,c])=><div className="health-row" key={a}><span>{a}</span><Status label={b} tone={c}/></div>)}</section><div className="notice">Causal recommendations remain disabled until merchant/action-specific randomized evidence passes its gate.</div></MerchantPage> }
